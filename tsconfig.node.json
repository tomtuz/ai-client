{
  "compilerOptions": {
    "composite": true,
    "module": "ESNext",

    // 'bundler' as we don't use file extensions with imports
    "moduleResolution": "Bundler",
    // vite -> esbuild requires this
    "isolatedModules": true,
    "types": ["node"]
  },
  "include": ["vite.config.ts"]
}
